import React, {Component} from 'react';

export default class Home extends Component {

    constructor(...args) {
        super(...args);
        this.state = {
        };
    }

    componentDidMount() {
    }

    render() {
        return (
            <div>这是一个详情</div>
        )
    }
}
